<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="org.autojs.permission.PLUGIN" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <meta-data
            android:name="org.autojs.plugin.WAKE_ACTIVITY"
            android:value="org.autojs.plugin.apkbuilder.template.impl.WakeActivity" />

        <activity
            android:name="org.autojs.plugin.apkbuilder.template.impl.WakeActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:finishOnTaskLaunch="true"
            android:permission="org.autojs.permission.PLUGIN"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="org.autojs.plugin.action.WAKE" />
            </intent-filter>
        </activity>

        <service
            android:name=".ApkBuilderTemplateInfoService"
            android:enabled="true"
            android:exported="true"
            android:permission="org.autojs.permission.PLUGIN">

            <intent-filter>
                <action android:name="org.autojs.plugin.INFO" />
                <category android:name="apk-builder-template" />
            </intent-filter>
        </service>

        <service
            android:name=".ApkBuilderTemplatePluginService"
            android:enabled="true"
            android:exported="true"
            android:permission="org.autojs.permission.PLUGIN">

            <intent-filter>
                <action android:name="org.autojs.plugin.APK_BUILDER" />
            </intent-filter>
        </service>

    </application>
</manifest>
